---
import { getCollection } from 'astro:content';
import WorkItem from './WorkItem.astro';

const works = await getCollection('work');
---

<div class="grid gap-4 w-full grid-cols-1 md:grid-cols-2 xl:grid-cols-3">
  {works.map((work) => (
    <WorkItem work={work} />
  ))}
</div>

